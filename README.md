# ngx-named-router
Provides named routing capabilities to your Angular project -- similar to Django's functionality -- so the user doesn't need to know the paths, and changing them won't require refactoring
